# ===========================================
# SERVER CONFIGURATION
# ===========================================
PORT=3000
NODE_ENV=development

# ===========================================
# DATABASE (PostgreSQL - Supabase)
# ===========================================
DB_HOST=your-project.supabase.co
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your-secure-password
DB_NAME=postgres

# ===========================================
# JWT AUTHENTICATION
# ===========================================
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRATION=7d

# ===========================================
# REDIS CACHE
# ===========================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_TTL=3600

# ===========================================
# CLOUDINARY (Image Storage)
# ===========================================
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=123456789012345
CLOUDINARY_API_SECRET=your-api-secret

# ===========================================
# GOOGLE MAPS API (Geocoding)
# ===========================================
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# ===========================================
# MERCADO PAGO (Payments)
# ===========================================
# Use TEST credentials for development:
# MP_ACCESS_TOKEN=TEST-xxxxxxxxxxxx
# MP_PUBLIC_KEY=TEST-xxxxxxxxxxxx
# Use PRODUCTION credentials for production:
MP_ACCESS_TOKEN=APP_USR-xxxxxxxxxxxx
MP_PUBLIC_KEY=APP_USR-xxxxxxxxxxxx
MP_CLIENT_ID=your-client-id
MP_CLIENT_SECRET=your-client-secret
MP_WEBHOOK_SECRET=your-webhook-secret

# ===========================================
# EMAIL SERVICE (Resend)
# ===========================================
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxx
EMAIL_FROM=noreply@yourdomain.com

# ===========================================
# APPLICATION URLS
# ===========================================
FRONTEND_URL=http://localhost:3001
API_URL=http://localhost:3000

# ===========================================
# OPTIONAL: Email Service Alternative (Nodemailer)
# ===========================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# ===========================================
# NOTES:
# ===========================================
# 1. Copy this file to .env and fill in your actual values
# 2. Never commit .env file to version control
# 3. Generate a strong JWT_SECRET using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 4. For production, use strong passwords and enable SSL for database connections
# 5. Ensure Redis is running before starting the application
# 6. PostgreSQL extensions required: cube, earthdistance
